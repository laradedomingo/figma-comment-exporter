{
  "name": "figma-comments-sync",
  "private": true,
  "type": "module",
  "engines": { "node": ">=20.6" },
  "scripts": {
    "sync": "node sync.mjs",
    "sync:local": "node --env-file=.env sync.mjs",
    "viewer": "node build-viewer.mjs",
    "sync:viewer": "LOCAL_ONLY=true node --env-file=.env sync.mjs && node build-viewer.mjs"
  },
  "dependencies": {
    "googleapis": "^181.0.0"
  }
}
